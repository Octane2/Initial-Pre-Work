/* For this module, I learned how return a random number using the Math.random()
function, which generates pseudo-random numbers ranging from 0 inclusive to 1
non-inclusive. */

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
