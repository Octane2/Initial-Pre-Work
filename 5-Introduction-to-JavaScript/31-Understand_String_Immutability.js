/* For this module, I learned that string values are immutable,
meaning that they can't be altered.  In order to change the value of
a string variable, an entirely new string must be assigned. */

// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World";
