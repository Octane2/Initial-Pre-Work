/* for this module, I learned how to simplify the process for returning
boolean values from functions.  Efficiency is the name of the game, and
reducing to two lines of code improves efficiency of the code. */

function isLess(a, b) {
  // Fixed code.
    return a < b;
}

// Change these values to test
console.log(isLess(10, 15));
