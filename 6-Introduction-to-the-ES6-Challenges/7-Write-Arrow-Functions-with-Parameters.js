/* For this module, I learned that arrow functions can accept arguments. */

//My solution for Part 7.
const myConcat = (arr1, arr2) => arr1.concat(arr2);
  "use strict";

// test your code
console.log(myConcat([1, 2], [3, 4, 5]));
